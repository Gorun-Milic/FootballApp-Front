import { IsSavedDirective } from './is-saved.directive';

describe('IsSavedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSavedDirective();
    expect(directive).toBeTruthy();
  });
});
